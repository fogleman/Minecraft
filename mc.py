import main

main.main()

"""
class World:
  
  def set_block(x, y, z, texture):
    t = (x, y, z)
    
    textureToPass = main.GRASS
    if isinstance(texture, str):
      if texture = "GRASS":
        
"""
